{
  "name": "gaon-sathi",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build && node -e \"const fs = require('fs'); if (fs.existsSync('apps/web/out')) { fs.cpSync('apps/web/out', 'out', { recursive: true }); console.log('Successfully copied apps/web/out to ./out for Vercel'); }\"",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "engines": {
    "node": "24.x",
    "npm": ">=10.x"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "npm@10.8.2"
}
